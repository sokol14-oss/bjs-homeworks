"use string";
function calculateTotalMortgage(percent, contribution, amount, date) {
  if (percent === ToString(percent) || contribution === ToString(contribution) || amount === ToString(amount) || date === ToString(date)) {
Number(percent, contribution, amount, date);
  }
  else {
  	let arr = [percent, contribution, amount, date];
  	for (let i = 0; i < arr.length; i++ ) {
  	arr.includes(percent, contribution, amount, date);
  }
  arr[i].ToString(Параметр $(arr[i]) содержит неправильное значение $(arr[i]).)
  console.log(arr[i].ToString());
  }
  let body = amount - contribution;
  Data = new Date();
  let payMonth = date.getMonth() - Data.getMonth() 
    + (12 * (date.getFullYear() - Data.getFullYear()));
    let p = (percent / 100) / 12;
   let pay = body * (p + p / (((1 + P)Math.pow(1 + p, payMonth)) - 1));
   let totalAmountn.toFixed(2) = pay * payMonth;
   console.log(Number.totalAmountn);

    return Number.totalAmountn;
}

function getGreeting(name) {
    if (name === true) {
    	console.log(`Привет, мир! Меня зовут ${name}.`);
    }
    else {
    	console.log(`Привет, мир! Меня зовут ${"Аноним"}.`)
    }
    return getGreeting;
}